/**
 * Shared frontend utilities and helpers.
 *
 * Use this module for cross-cutting helpers that are not UI-specific
 * (formatting, constants, config accessors). UI class merging lives in utils.ts.
 */

export const APP_NAME = "AI Engineering Lab" as const;
